Basic logical operations:
AND = .
OR = +
NOT = overbar (-)

De morgans theoram :
    not(A.B) = not A + not B
    not(A+B) = not A . not B
    
Important simplifications:
~a + ab = ~a + b    (can be proved by taking complement)
a + ~ab = a + b